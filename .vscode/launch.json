{
  "version": "0.2.0",
  "configurations": [
    {
      "type": "brightscript",
      "request": "launch",
      "name": "BrightScript Debug: Launch",
      "host": "${env:ROKU_DEV_TARGET}",
      "password": "${env:ROKU_DEV_PASSWORD}",
      "rootDir": "${workspaceFolder}/DetailsScreen",
      "envFile": "${workspaceFolder}/.env",
    }
  ]
}
